/**
  * Use this sheet to test your ideas rapidly
  **/

val result: Vector[Int] = Vector()
result.forall(r => r > 1)
result.exists(r => r > 1)

val map = Map(1->"a", 2 -> "b")

map ++ Map(1 -> "c", 3 -> "d")

1.2.round

-1.4.toInt

1.2.toInt

Vector(0, 1, 2, 3, 4, 5).slice(0, 2)