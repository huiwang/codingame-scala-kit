import scala.util.Random

/**
  * Use this sheet to test your ideas rapidly
  **/

Map(1 -> 1, 3 -> 2) ++ Map(1 -> 2, 2 -> 1)


Vector(1, 2,3).combinations(2).toVector

val seed = 1238491235712L
val random = new Random(seed)
random.nextLong().abs % 5
random.nextLong().abs % 5
random.nextLong().abs % 5
random.nextLong().abs % 5
random.nextLong().abs % 5
random.nextLong().abs % 5
random.nextLong().abs % 5
random.nextLong().abs % 5
