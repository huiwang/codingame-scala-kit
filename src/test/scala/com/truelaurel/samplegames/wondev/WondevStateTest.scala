package com.truelaurel.samplegames.wondev

import com.truelaurel.math.geometry.Pos
import com.truelaurel.samplegames.wondev.domain.WondevState
import org.scalatest.{FlatSpec, Matchers}

class WondevStateTest extends FlatSpec with Matchers {
//  "WondevState" should "compute height" in {
//    val state = WondevState(0, Seq(
//      "000",
//      ".01"))
//
//    state.height(Pos(2, 1)) shouldBe Some(1)
//    state.height(Pos(0, 1)) shouldBe None
//    state.height(Pos(1, 1)) shouldBe Some(0)
//  }
//
//  it should "apply an action" in {
//    val state = WondevState(0, Seq(
//      "000",
//      ".01"),
//      myUnits = Seq(Pos(1, 0)))

//    val after = state.apply(LegalAction("", 0, "SE", "NW"))
//    after.myUnits.head shouldBe Pos(2, 1)
//    after.height(Pos(1, 0)) shouldBe Some(1)
//
//  }
}
